<% if signed_in? %>
     <div class="row">
      <aside class="span4">
        <section>
           <%= render 'shared/user_info' %>
        </section>
        <section>
           <%= render 'shared/stats' %>
        </section>
        <section>
           <h1>What's up?</h1>
           <%= render 'shared/micropost_form' %>
        </section>
      </aside>
      <div class="span8">
           <h3>Micropost Feed</h3>
           <%= render 'shared/feed' %>
       </div>
    </div>
<% else %>
 <div class="center hero-unit">
  <h1>Social Blogs</h1>
   <p>Huhu, hallo, ich erkläre gerade der Conny, wie das funktioniert!
     This is the home page for Socials blogs, an application 
     created with Ruby on Rails by expanding the sample app of the
     <a href="http://railstutorial.org/">Ruby on Rails Tutorial</a>

   </p>
  <%= link_to "Sign up now!", signup_path, class: "btn btn-large btn-primary" %>
 </div>
<% end %>
